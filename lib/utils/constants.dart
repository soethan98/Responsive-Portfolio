import 'package:flutter/material.dart';

const Color cardBg = Color(0xFFF5F5F5);
const Color lightScaffoldBackground = Color(0xFFEFEFEF);
const Color darkLight = Color(0xFF212121);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1); // we use it on our animation
const maxWidth = 1440.0; // max width of our web

const Color lightPrimary = Color(0xFFf4f4f4);
const Color darkPrimary = Colors.black;

const Color lightAccent = Colors.blue;
// const Color darkAccent = Colors.cyanAccent;

const Color lightBG = Colors.white;
const Color darkBG = Color(0xFF1f1f1f);
const Color white = Colors.white;
